import 'package:flutter/material.dart';


class TextCompletionPage extends StatelessWidget {
  const TextCompletionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("TextCompletionPage"),
      ),
    );
  }
}
